# Worked example -- initialize-srs

This example shows how the `01-initialize-srs` skill consumes a minimal
`_context/` slice and produces the IEEE 830 skeleton at
`projects/<ProjectName>/02-requirements-engineering/srs/0-skeleton.md`.

The inputs come from the `healthcare-saas` golden-path example; the
expected output demonstrates the mandatory front matter, the BG-to-
section-1 traceability wiring, and the placeholder markers that flag
which downstream skill is responsible for each subsection.

# MVP: expand later

<!-- MVP: expand with additional scenarios -->
